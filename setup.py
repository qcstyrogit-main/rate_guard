from setuptools import setup, find_packages

setup(
    name="rate_guard",
    version="0.0.1",
    description="Enforces Allow Rate permission — hides financial figures from unauthorized users",
    author="QC Styropackaging Corporation",
    author_email="",
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[],
)
